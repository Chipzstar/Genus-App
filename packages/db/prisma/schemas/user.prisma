model User {
  id        Int         @id @default(autoincrement())
  clerk_id  String      @unique
  email     String      @unique
  firstname String
  lastname  String
  posts     Post[]
  groups    GroupUser[]
  messages  Message[]
  threads   Thread[]
  comments  Comment[]
}
